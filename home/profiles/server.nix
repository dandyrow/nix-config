# Server-specific Home Manager configuration.
# Import alongside home/default.nix for headless server machines.
#
# Typically a minimal subset of the desktop config — no GUI tools,
# lighter shell config, etc.
{ ... }: {
}
