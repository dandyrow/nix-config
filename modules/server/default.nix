# Server NixOS configuration.
# Imported by headless server/VM hosts.
#
# Add server-specific system config here:
# SSH hardening, minimal package set, firewall rules,
# container runtime (podman/docker), etc.
{ ... }: {
}
